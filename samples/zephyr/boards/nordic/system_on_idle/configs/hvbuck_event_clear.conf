# Clear HVBUCK mode-change events before each idle window. This is the only
# write this sample makes to the buck converter's own peripheral state, so
# it is opt-in: without this fragment, the diagnostics build only reads
# HVBUCK, never writes to it.
CONFIG_NRF7120_HVBUCK_EVENT_CLEAR=y
